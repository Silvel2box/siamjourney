---
name: project-siamdiscovery
description: "SiamDiscovery — เว็บท่องเที่ยวไทย 77 จังหวัด, stack + business model ที่ตัดสินใจแล้ว"
metadata: 
  node_type: memory
  type: project
  originSessionId: 916fd74d-e045-4a60-a873-50f093554e44
---

เว็บท่องเที่ยวไทย "SiamDiscovery/SiamExplore" จัดเนื้อหาแบบ ภาค → จังหวัด(77) → หมวด(สถานที่ท่องเที่ยว/ร้านอาหาร/คาเฟ่/สินค้า OTOP). เริ่มภาษาไทยก่อน วางโครงรองรับ i18n (`/en/` + hreflang) ทีหลัง — ไม่พึ่ง Google Translate widget เพราะเสีย SEO.

**ตัดสินใจแล้ว (2026-07-14):**
- Stack: **Next.js (App Router, TypeScript) + Tailwind** — เลือกแทน Astro เพราะต้องการ portal ร้านค้า/คอมมิชชั่น (เฟส 2) อยู่ในแอปเดียวกัน และรับได้กับการรัน Node runtime บน Plesk (Passenger). ดึงข้อดี SEO ของ Astro มาด้วย: default = React Server Components (near-zero client JS), `generateStaticParams` gen หน้า 77 จังหวัด + place เป็น static, Metadata API + hreflang, app/sitemap.ts + robots.ts, JSON-LD schema.org. กฎโปรเจกต์: default Server Component เสมอ, ใส่ `"use client"` เฉพาะที่จำเป็น (search, mobile menu). markdown loader เอนไปทาง **Velite** (type-safe, เบา) — ยังไม่ล็อกสุดท้าย.
- Deploy Plesk: เฟส 0-1 เนื้อหา static แต่รันเป็น Next app (`next start`) เตรียมรับ API เฟส 2. next/image บน Plesk ต้องตั้ง loader หรือ unoptimized.
- Monetization MVP: **Affiliate-first** (Agoda/Booking/Shopee/Lazada) ผ่าน `<AffiliateButton>`, เว้น `<AdSlot>` ไว้รอ AdSense, ใส่ field `sponsored: 0/1/2` ใน frontmatter เตรียมระบบขายพื้นที่/คอมมิชชั่นทีหลัง (ยังไม่ทำ backend ร้านสมัครในเฟสแรก).

**ที่มา:** เริ่มจากไฟล์เดียว `thailand_travel_template.html` (Tailwind CDN, landing page โทนทอง/ดำ, ฟอนต์ Kanit+Prompt) → จะกลายเป็น BaseLayout + components.

**เฟส 0 (MVP) — เสร็จแล้ว (2026-07-14):** โปรเจกต์จริงอยู่ที่ `E:\Doc-IT\claude-workspace\siamjourney` (แยกจาก siamdiscovery ซึ่งเหลือแค่ template เดิม). Stack ที่ได้จริง: **Next.js 16.2.10 (App Router, Turbopack) + React 19 + Tailwind v4 + TypeScript**. Content loader = **gray-matter + zod + marked** (ไม่ใช้ Velite เพราะ Turbopack เป็นดีฟอลต์ + phase 0 เล็ก — เปลี่ยนกลับได้ถ้า content เยอะ). ยืนยันชื่อโดเมน **siamjourney.com** (siam ไม่ใช่ saim).

โครง: `lib/` (site.ts config, regions.ts 6 ภาค, categories.ts 4 หมวด, content.ts loader+zod schema). `content/provinces/*.md` + `content/places/*.md` (frontmatter validate ด้วย zod; place มี field affiliate{label,url} + sponsored 0/1/2). `components/` (Navbar+ScrollReveal+ProvinceSearch เป็น client, ที่เหลือ server). `app/` routes: `/`, `/[region]`, `/[region]/[province]`, `/place/[slug]` (ทุกหน้า generateStaticParams + dynamicParams=false = static), sitemap.ts, robots.ts. SEO: generateMetadata + JSON-LD schema.org (TouristAttraction/Restaurant/CafeOrCoffeeShop/Product ตาม category). Tailwind v4 theme (สี primary #c28e46, ฟอนต์ Kanit+Prompt) อยู่ใน `app/globals.css` @theme. FontAwesome โหลดผ่าน CDN link ใน layout head. next.config.ts pin turbopack.root กัน lockfile ของโฟลเดอร์แม่. `npm run build` ผ่าน → 22 static pages. ข้อมูล seed: ภาคเหนือ เชียงใหม่+น่าน อย่างละ 4 สถานที่ (ครบ 4 หมวด). รูปเป็น Unsplash placeholder, affiliate URL เป็น placeholder (Klook/Shopee) รอใส่ tag จริง.

**เลื่อนไปเฟสถัดไป:** หน้า category เดี่ยว (`/[region]/[province]/[category]`), next/image (ตอนนี้ใช้ <img> ธรรมดา — บน Plesk ต้องตั้ง loader/unoptimized ถ้าจะใช้), i18n /en/, ครบ 77 จังหวัด, ระบบร้านสมัคร/คอมมิชชั่น (backend), newsletter form ยังเป็น placeholder ไม่มี backend.

**Deploy Plesk:** `npm run build` + `npm start` (next start) ผ่าน Passenger/Node. ยังไม่ได้ตั้ง deploy จริง.

**Database — เริ่มแล้ว (2026-07-15):** ทดสอบ DB บน dev ด้วย **XAMPP MySQL** (parity กับ Plesk MariaDB). ใช้ **Prisma 6** (ไม่ใช่ 7 — v7 เอา url ออกจาก schema บังคับ prisma.config.ts+driver adapter ซับซ้อนเกิน; downgrade เป็น 6.19). schema `Subscriber` (email unique), `lib/prisma.ts` singleton, migrate init สำเร็จ, `postinstall: prisma generate`. ฟีเจอร์ DB ตัวแรก = **newsletter signup** (Server Action `app/actions/newsletter.ts` + client `NewsletterForm.tsx` useActionState → Prisma create, กันซ้ำด้วย P2002). verify: create/read/duplicate/render ผ่าน (ยังไม่ browser-click test — ต้อง browser). DATABASE_URL อยู่ใน `.env` (gitignored, root มีรหัสที่ผู้ใช้ตั้งเอง — อย่า echo). **XAMPP default root ว่างถูกปฏิเสธ → ผู้ใช้ตั้งรหัส root เอง.** Plesk deploy: สร้าง DB+user, ตั้ง DATABASE_URL, `prisma migrate deploy` (ไม่ใช่ dev). หน้า content ยัง static เหมือนเดิม (hybrid). 

**ประวัติเดิม:** เฟส 0-1 **ไม่ใช้ DB** (ตั้งใจ) — content เป็น markdown + zod, generate static. ยืนยัน 2026-07-14: ช่วงแรกผู้ใช้กับทีม dev กรอก content เอง ไม่มีทีม non-dev มาแก้ผ่าน admin จึงไม่ต้องดึง DB มาเร็ว. DB (MySQL/MariaDB + Prisma บน Plesk) เข้าเฟส 2 ตอนมี runtime writes (ร้านสมัคร, คอมมิชชั่น, คลิก affiliate, newsletter). schema Zod ที่วางไว้ map เข้าตาราง DB ได้ตรงๆ เมื่อ migrate.

**ความคืบหน้า:** เฟส 1.1 **เสร็จครบ 77 จังหวัด** (2026-07-15) — ทุกภาค (เหนือ 9/อีสาน 20/กลาง 22/ตะวันออก 7/ตะวันตก 5/ใต้ 14), 308 places (ทุกจังหวัดครบ 4 หมวด), build ผ่าน 397 static pages. verify integrity สะอาด (0 slug/category/province-ref ผิด) + affiliate 154 (attraction Klook + otop Shopee). featured 8 จังหวัด. มี `content/README.md` เป็นคู่มือทีม. แก้ bug regions.ts กลาง 21→22. เนื้อหา 68 จังหวัดที่เหลือใช้ subagent 5 ตัว (general-purpose) ทำขนานกันตามภาค — เป็นแพตเทิร์นที่ใช้ซ้ำได้ถ้าต้อง generate เนื้อหาจำนวนมาก. เฟส 1.2 เสร็จ (2026-07-15): หน้า category `/[region]/[province]/[category]` (308 หน้า, generateStaticParams + dynamicParams=false, generateMetadata "<หมวด>ใน<จังหวัด>", ItemList JSON-LD, sibling nav), ลิงก์ "ดูทั้งหมด" จากหน้าจังหวัด, เพิ่มใน sitemap (700 URLs รวม). build 705 pages ผ่าน, invalid path → 404. เฟส 1.3 เสร็จ (2026-07-15): แปลง <img> ทั้งหมด → next/image (fill+sizes+alt) ใน PlaceCard/ProvinceCard/PageBanner/หน้าแรก; next.config.ts images.remotePatterns=unsplash, optimizer verified 200 บน `next start` (Plesk-ready, fallback unoptimized 1 บรรทัด). เจอ+แก้บั๊ก: 4 URL placeholder จาก Unsplash เป็นลิงก์ตาย (404) กระจาย 90 ไฟล์ — next/image เปิดโปง (ตอนใช้ <img> รูปแตกเงียบ) — แทนด้วยรูป alive แล้วสแกนยืนยัน 12 unique URL alive หมด. **บทเรียน: verify รูปด้วยการ fetch จริงเสมอ อย่าเชื่อว่า URL ใช้ได้.** งานทีมที่เหลือ: แทน placeholder ด้วยรูปจริง 308 แห่ง (แก้ field image:). เฟส 1.4 เสร็จ (2026-07-15): ระบบ affiliate กลาง `lib/affiliate.ts` (config networks klook/agoda/booking/shopee/lazada + `buildAffiliateUrl(rawUrl, placeSlug)`), AffiliateButton รับ placeSlug แล้วเติม sub-id ต่อสถานที่ (per-place click attribution) + utm_source=siamjourney/utm_medium=affiliate อัตโนมัติ; ถ้ากรอก id จริงใน config จะเติม affiliate id param ให้ด้วย โดยไม่ต้องแก้ markdown 154 จุด. verify: klook→aff_sub, shopee→af_sub_siteid ถูกต้อง, rel=sponsored nofollow ครบ. งานผู้ใช้: สมัครโปรแกรม affiliate แล้วกรอก id + ตรวจชื่อ param. เฟส 1.5 เสร็จ (2026-07-15): Breadcrumb JSON-LD (ทำใน PageBanner ครอบทุก inner page), OG image ต่อหน้า (place/province/category=รูปหน้านั้น, region/home=site.ogImage), canonical ครบทุกหน้า (home มี metadata export เอง เลี่ยง footgun การ inherit ที่ root), เพิ่ม lat/lng (zod optional) + geo JSON-LD GeoCoordinates + ฝัง Google Map iframe (output=embed, ไม่ต้อง API key) + ลิงก์เปิด Maps ในหน้า place. hreflang จริงรอ /en/ เฟส 3 (โครง alternates พร้อม). เฟส 1.6 เสร็จ (2026-07-15): หน้า /privacy + /terms (แบบร่าง template ครอบ affiliate/ads/cookies/PDPA + disclaimer ให้ทนายตรวจ, ใน sitemap), custom 404 app/not-found.tsx (ธีมทอง/ดำ, verify 404+content).

**★ เฟส 1 เสร็จครบทั้งหมด (1.1-1.6) — engineering (2026-07-15):** build 707 static pages. เว็บ content-only (ไม่มี DB) พร้อมใช้: 77 จังหวัด/308 places/308 category pages, next/image, affiliate กลาง, SEO ครบ (metadata/canonical/JSON-LD breadcrumb+geo+ItemList/OG/sitemap/robots), privacy/terms/404. **เหลืองาน content/บัญชีทีม:** รูปจริง 308 แห่ง, affiliate ID จริง, พิกัด lat/lng (ทางเลือก), ทนายตรวจ legal. **ขั้นถัดไป: Deploy Plesk หรือ เฟส 2 (ระบบร้านค้า/คอมมิชชั่น + DB MySQL/Prisma).** ผู้ใช้ให้อนุมัติถาวรทุก action ในโปรเจกต์นี้ (ไม่ต้องขอ permission เพิ่ม).

**เฟส 2 — ร้านค้าสมัคร + auth เสร็จ (2026-07-16):** เริ่มงานเฟส 2 ตัวแรก (ผู้ใช้เลือก #2 ก่อน, ให้แนะนำ auth). **แนวทางที่ตัดสิน: custom auth + DB session, ไม่เพิ่ม npm dependency เลย** (ยึด karpathy simplicity + เลี่ยง version-lag เสี่ยงบน Next 16 แบบที่เจ็บกับ Prisma 6/7 — ไม่ใช้ NextAuth). อ่าน Next 16 auth guide ใน `node_modules/next/dist/docs/01-app/02-guides/authentication.md` ก่อนเขียน: `cookies()` เป็น async, middleware เปลี่ยนชื่อไฟล์เป็น `proxy.ts` (รัน Node runtime), optimistic proxy check เป็น optional — เราข้าม proxy.ts ใช้ DAL guard แทน (secure-by-default). Models: `Merchant` (email unique/passwordHash/shopName/status pending|approved|suspended — รวม owner+shop เป็นตัวเดียวตามที่ผู้ใช้เลือก, ไม่แยก Shop จนกว่าจะมี requirement) + `Session` (id = opaque random token เก็บใน cookie, onDelete cascade). `lib/auth.ts`: hash/verify ด้วย **`node:crypto` scrypt** (native, เลี่ยง bcrypt native binding ที่ build ยากบน Plesk/Passenger), createSession/deleteSession ตั้ง httpOnly cookie `shop_session` (secure=prod, 30 วัน), `getMerchant()` = React `cache` DAL คืน DTO (select ไม่เอา passwordHash) + ลบ session หมดอายุ, `requireMerchant()` redirect /login. `app/actions/auth.ts` register/login/logout (Zod, P2002=email ซ้ำ, redirect นอก try). หน้า `app/(shop)/{register,login,dashboard}` + `components/AuthForm.tsx` (client useActionState, reuse 2 โหมด). dashboard = **stub** (logout + status badge) — แก้ข้อมูลจริง/แพ็กเกจจ่ายเงิน/admin approval ยังไม่ทำ (กัน scope creep). verify (ยิงจริงบน `next start` + script DB): guard redirect, valid cookie→dashboard 200, invalid→/login, login-แล้ว→dashboard, hash round-trip/reject wrong, P2002, session expiry+cascade — ผ่านหมด. build 710 pages (dashboard/login/register = dynamic ƒ เพราะอ่าน cookie). **บทเรียน verify: server action ยิงตรงยาก (Next-Action protocol) → พิสูจน์ cookie→session→merchant ด้วยการ forge session row ใน DB แล้ว curl พร้อม Cookie header แทน. Prisma Client ไม่โหลด .env runtime เอง (ต่างจาก CLI) — script standalone ต้อง source .env ก่อน + ต้องรันในโฟลเดอร์ project ถึง resolve @prisma/client ได้.**

**Phase 3+4 — admin + shop profile เสร็จ (2026-07-17):** git "Phase 3" = admin role (`Merchant.role` merchant|admin, `requireAdmin()` guard signed-out→/login non-admin→/dashboard, `/admin` อนุมัติ/ระงับ/คืนสถานะ, `updateStatus` action, `prisma/seed.mjs` promote `ADMIN_EMAIL`→admin). git "Phase 4" = shop profile: เพิ่ม fields ใน Merchant (description/province/category/address/phone/website/image, เก็บ province/category เป็น **slug** ตรงกับ places), `updateShop` action (zod, เอา id จาก session ไม่เชื่อฟอร์ม), `ShopForm` (useActionState prefill + dropdown จังหวัด77/หมวด4 จาก getAllProvinces/categories), dashboard แทน stub เป็นฟอร์มแก้, หน้าสาธารณะ `/shop/[id]` (**dynamic** อ่าน DB, approved-only→404, รูปใช้ `<img>` ธรรมดาเพราะ URL อิสระ next/image remotePatterns มีแค่ unsplash). owner+shop รวมเป็น Merchant เดียว (ไม่แยก Shop). Navbar เพิ่มลิงก์ "สำหรับร้านค้า"→/dashboard. **write path (ปุ่ม save) verify local ไม่ได้เพราะ Next server-action wire protocol ยิงตรงไม่ได้ — พิสูจน์จริงตอน deploy (กดในเบราว์เซอร์ผ่าน).**

**★ DEPLOYED LIVE (2026-07-18) — https://siam-journey.com (Plesk/Passenger/MySQL).** เข้าถึง Plesk ผ่าน UI + Git เท่านั้น (ไม่มี SSH → build/migrate/seed รันผ่าน "Run script" = npm scripts: `migrate:deploy`/`migrate:reset`/`db:seed`). Startup = **`server.js`** (custom server CommonJS `require('next')` + listen PORT — Passenger ไม่ hook `next start` ตรง; อ่าน doc Next 16 ยืนยัน custom server ใช้ได้เพราะไม่ใช้ standalone output). **บทเรียน deploy ที่เจ็บ (แก้แล้ว ดู memory `plesk-nextjs-deploy-gotchas`):** (1) **casing ตาราง Windows→Linux**: migration เก่า CREATE `Merchant`(ตัวใหญ่ตาม model) แต่ ALTER `merchant`(ตัวเล็ก generate บน XAMPP ที่ lower_case_table_names=1) → Linux case-sensitive พัง error 1146 → แก้ด้วย `@@map("merchant"|"session"|"subscriber")` ตัวเล็ก + squash 4 migration เป็น baseline เดียว (pre-launch ไม่มี prod data). (2) **`.env` แยกสำหรับ CLI**: Plesk custom env vars ถึงแค่ runtime app (Passenger) ไม่ถึง "Run script" → Prisma CLI หา DATABASE_URL ไม่เจอ → ต้องสร้าง `.env` ใน Application Root ให้ CLI โหลดเอง. (3) **ModSecurity react2shell (CVE-2025-55182/CVE-2025-66478) บล็อก Server Action ทุกตัว**: WAF จับ `"bound":"$@1"` Flight sentinel ของ useActionState เป็น RCE → 403 → แก้ที่ Plesk WAF (ModSecurity) หน้า domain → "Switch off security rules" → Security rule IDs ใส่ `1055182000-1055182099` (คง mode On, ปิดเฉพาะกฎ react2shell). stack เรา patched แล้ว (react 19.2.4≥19.2.1, next 16.2.10≥16.0.7) จึงเป็น false-positive ปิดปลอดภัย. `.htaccess` SecRuleRemoveById ใช้ไม่ได้ (โฮสต์ล็อก override → 500). DATABASE_URL prod = `mysql://mutelu_siamadmin:***@localhost:3306/mutelu_siamjourney`, ADMIN_EMAIL/admin = dev.silver13@gmail.com. verify นอก: `/`, `/[region]/[province]`, `/shop/1` (approved) = 200; register→dashboard→save→/shop→/admin ผ่านครบบน prod.

**Roadmap/To-do:** อยู่ในไฟล์ `siamjourney/TODO.md` (source of truth ของ task list — เฟส 1 เนื้อหา+SEO, เฟส 2 monetization+backend ร้านค้า, เฟส 3 i18n/ขยาย, + deploy Plesk). อัปเดตไฟล์นั้นเมื่อความคืบหน้าเปลี่ยน.

**AdSense verify ผ่าน + แก้บั๊กโดเมน (2026-07-21):** ใส่ publisher ID จริง `ca-pub-1938381370106852` ใน `lib/adsense.ts` แล้ว **ยืนยันความเป็นเจ้าของเว็บกับ AdSense ผ่าน** (ขั้นถัดไป = กด "ขอให้มีการตรวจสอบ" รออนุมัติ แล้วค่อยใส่ slot ID). แยกสวิตช์เป็น 2 ชั้น: `adsenseEnabled` (มี client → loader script ขึ้น พอสำหรับ verify/review) กับ `adUnitsEnabled` (ต้องมี slot ด้วย → ค่อย render `<ins class="adsbygoogle">`) เพราะถ้าเปิดสวิตช์เดียวจะได้ `<ins data-ad-slot="">` เสียๆ 4 จุดบนเว็บที่กำลังถูกตรวจ. เพิ่ม `public/ads.txt`. **★ บทเรียนสำคัญ: `next/script` (Next 16 App Router) ไม่ปล่อย `<script>` ลง server HTML เลย — ได้แค่ `<link rel="preload" as="script">` แล้ว inject ตัวจริงตอน hydrate ทั้ง `afterInteractive` และ `beforeInteractive` (doc บอกว่า beforeInteractive "injected into the initial HTML from the server" แต่ build จริงไม่ใช่ — doc ไม่ตรงของจริง). เบราว์เซอร์ใช้ได้แต่ crawler/verifier ที่อ่าน HTML ดิบมองไม่เห็น → AdSense ขึ้น "ยืนยันเว็บไซต์ของคุณไม่ได้". แก้ด้วยการเขียน `<script async src=... crossOrigin>` ดิบใน `<head>` ของ `app/layout.tsx` (React 19 render ลง SSR output ตรงๆ) — verify ด้วยการ grep HTML ที่ build ออกมา/curl ไลฟ์ ไม่ใช่ดูในเบราว์เซอร์.** 🚨 **บั๊กที่เจอพ่วง: `lib/site.ts` ตั้ง `url/domain = siamjourney.com` (ไม่มีขีด) มาตลอด แต่โดเมนจริงคือ `siam-journey.com`** — siamjourney.com เป็นของคนอื่น (resolve 3.33.251.168 → เด้ง 128.12.146.165) ทำให้ canonical/OG/sitemap ทั้ง 707 URL ประกาศว่าเว็บตัวจริงอยู่ที่โดเมนคนอื่น = เหตุผลที่ไม่ติด index. แก้ `site.ts` แล้ว (+ `email: hello@siam-journey.com` — **ยังต้องสร้างเมลบ็อกซ์ใน Plesk**). ค้างไว้ (ยังไม่ทำ): ยังไม่เคยสร้าง property ใน Google Search Console เลย (แนะนำ Domain property + DNS TXT), `www.siam-journey.com` ตอบ 200 ไม่ redirect → ควรตั้ง 301 → non-www ใน Plesk.

**เฟส 2D-b — ระบบโรงแรม เสร็จ (2026-07-24):** ผู้ใช้เลือกทำ "ระบบโรงแรม" ก่อน (2D-a จัดการร้านค้าเต็ม ยังค้าง). ออกแบบ **`Hotel` = สำเนา `Place` ลบ `category`** (โรงแรมไม่อยู่ใน taxonomy 4 หมวด) เพื่อ reuse โค้ดสูงสุด: reuse `affiliate {label,url}` JSON (ปุ่มจอง → AffiliateButton + buildAffiliateUrl เดิม รองรับ agoda/booking อยู่แล้ว), `imageCredit` JSON, `sponsored`, `lat/lng`, `priceRange`, `address`; ตัด `hours` (โรงแรม 24 ชม.); ไม่ใส่ `starRating` v1. `@@map("hotel")` ตัวเล็ก (บทเรียน casing Windows→Linux). migration `add_hotel`. `lib/content.ts` เพิ่ม Hotel DTO + `toHotel` + cached `getAllHotels/getHotel/getHotelsByProvince` + generalize `bySponsorThenName` เป็น structural type `{sponsored,name}` ใช้ร่วม place+hotel. Admin CRUD `/admin/hotels` (list+filter+new+edit) + `HotelForm` (mirror PlaceForm ลบ category/hours) + actions `saveHotel/deleteHotel` — **extract helper `affiliateFrom(fd)`** (rule-of-three: place+hotel; savePlace อัปเดตมาใช้ helper ด้วย) + `revalidateHotelPaths` (revalidate /hotel/[slug], /hotel, หน้าจังหวัด, sitemap) + AdminNav tab "ที่พัก" + `deleteProvince` guard นับ hotel เพิ่ม (กัน orphan). หน้าสาธารณะ: `/hotel` index (จัดกลุ่มตามจังหวัด, ISR 3600) + `/hotel/[slug]` detail (LodgingBusiness JSON-LD, แผนที่ iframe, ปุ่มจอง) + `HotelCard` + เซคชัน "ที่พัก" ในหน้าจังหวัด (internal linking กัน SEO island — เดิม /hotel จะเป็นเกาะไม่มีลิงก์เข้า) + Navbar link + sitemap. **Route safety:** `/hotel` (static) ชนะ `/[region]` (dynamic) เหมือน `/place` ที่ทำอยู่. verify (next dev + seed test hotel ชั่วคราวแล้วลบ): index/detail/province-section render, unknown→404, admin guard anon→307 ทั้ง 3 route, sitemap มี /hotel+detail, affiliate href auto = `?tag=<slug>&utm_source=siamjourney&utm_medium=affiliate` (agoda subIdParam=tag). build ผ่าน (/hotel=Static, /hotel/[slug]=SSG dynamicParams, /admin/hotels/*=dynamic). **write path (กด save จริง) verify ตอน deploy ในเบราว์เซอร์เท่านั้น** (Next-Action curl ตรงไม่ได้ เหมือน place/province). **DEPLOY 2D-b = มี migration ใหม่: pull → NPM install (postinstall=prisma generate ให้ client รู้จักตาราง hotel) → `migrate:deploy` → build → restart** (ห้ามข้าม install/migrate). 🐛 ระหว่างทำเจอ node `server.js` local ค้าง 5 ตัว (อายุ 1-2 วัน) ล็อก Prisma query-engine DLL ทำ `prisma generate` EPERM — หยุด process แล้ว generate ผ่าน (prod จริงบน Plesk ไม่กระทบ).

**แกลเลอรีรูปโรงแรม เสร็จ (2026-07-24, ต่อจาก 2D-b):** ผู้ใช้อยากให้โรงแรมมีรูปหลายมุม (รูปหลักเดิม=hero ยังอยู่). ตัดสินใจ scope: **แกลเลอรีรูปอย่างเดียว** (ไม่มีรีวิวข้อความ/ดาว — งานนั้นใหญ่กว่ามาก เก็บไว้ทีหลัง), **โรงแรมก่อน** (places ขยายทีหลังได้), **caption optional**. Data = **`gallery Json?` = `[{url, caption?}]` บน Hotel** (ไม่สร้างตาราง `HotelImage` — reuse JSON pattern เหมือน imageCredit/affiliate เพราะแกลเลอรีอ่านทั้งก้อนพร้อมโรงแรมเสมอ ไม่เคย query รูปเดี่ยว = ตาราง relational จะ over-engineer), cap 20, migration `add_hotel_gallery`. `lib/content.ts` เพิ่ม type `GalleryImage` + `gallery?` ใน Hotel DTO + `toHotel` map. `saveHotel` เพิ่ม helper `galleryFrom(fd)` (JSON.parse + zod `array({url,caption?}).max(20)`, ว่าง→Prisma.DbNull). Admin: `components/admin/GalleryField.tsx` (client) — อัปโหลด (reuse `/api/admin/upload` endpoint เดิม 100% ที่ resize ≤1500px) หรือวาง URL, ลบ, ↑↓ จัดลำดับ (swap index ไม่พึ่ง drag lib), caption ต่อรูป → เก็บ state array แล้ว serialize เป็น JSON ยัด hidden input `name="gallery"` (server action อ่านจาก FormData ได้). Public: `components/HotelGallery.tsx` (client) — กริดรูปย่อ (next/image) + **lightbox เต็มจอ** (state activeIndex, prev/next, Esc/ArrowLeft/Right keydown, lock body scroll, ไม่พึ่ง lib; รูปย่อ=next/image, รูปเต็มใน lightbox=plain img เพราะ dims อิสระ) — render เฉพาะเมื่อ gallery.length>0 (hero แยกใน PageBanner ไม่ซ้ำในกริด "รูปเพิ่มเติม"). JSON-LD LodgingBusiness `image` = array [hero, ...gallery urls] เมื่อมี gallery (schema.org รับ array). verify (next dev + seed hotel 3 รูป): กริด+caption render, JSON-LD image[]=hero+3=4, admin edit guard 307, build ผ่าน; **lightbox interaction (คลิก→overlay) = client JS ต้อง browser-test ตอน deploy** (เหมือน write path). **DEPLOY = มี migration ใหม่: pull → NPM install → `migrate:deploy` → build → restart.**
